[
  {
    "id": "git-committed-to-main",
    "tier": "verified",
    "technology": "Git",
    "category": "History & Undo",
    "title": "I committed to main by mistake",
    "summary": "The commit is on the wrong branch. What you do next depends entirely on whether it has been pushed.",
    "aliases": [
      "committed to main",
      "wrong branch",
      "committed accidentally",
      "undo commit",
      "committed by mistake",
      "move commit to branch",
      "oops committed"
    ],
    "verified_on": "2026-09-11",
    "verified_against": "git 2.45",
    "verified_by": "dinesh",
    "source": "https://git-scm.com/docs/git-reset",
    "inputs": [
      {
        "key": "branch",
        "label": "Branch you meant to use",
        "example": "feature/checkout-retry"
      },
      {
        "key": "remote",
        "label": "Remote",
        "example": "origin"
      }
    ],
    "steps": [
      {
        "title": "Find out whether the commit has left your machine",
        "cmd": "git status -sb && git log --oneline -3",
        "risk": "safe",
        "look": "The first line. 'ahead 1' means it is still local and you have easy options. If it is not ahead, it is already on the remote and rewriting is now other people's problem too.",
        "signals": [
          {
            "match": "ahead \\d+",
            "means": "Still local. You can move it cleanly.",
            "goto": "cause:local-only"
          },
          {
            "match": "\\[origin/main\\]\\s*$",
            "means": "Already pushed. Do not rewrite shared history.",
            "goto": "cause:already-pushed"
          }
        ]
      },
      {
        "title": "Move the commit onto the branch it belonged on",
        "cmd": "git branch {branch} && git reset --hard HEAD~1 && git switch {branch}",
        "risk": "destructive",
        "note": "reset --hard discards every uncommitted change in your working tree. Run git stash first if you have any. This is only safe because git branch {branch} was created on the line before, so the commit itself survives on that branch.",
        "look": "After this, main is back where it was and your commit sits on {branch}.",
        "signals": [
          {
            "match": "(?i)error: .*already exists",
            "means": "That branch already exists. Pick another name or switch to it and cherry-pick instead.",
            "goto": "cause:branch-exists"
          }
        ]
      },
      {
        "title": "Confirm nothing was lost",
        "cmd": "git log --oneline -3 {branch} && git log --oneline -3 main",
        "risk": "safe",
        "look": "Your commit on {branch}, and main matching the remote. If anything looks wrong, git reflog still has every prior position for about 90 days."
      }
    ],
    "causes": [
      {
        "id": "local-only",
        "cause": "The commit exists only in your clone.",
        "fix": "Create the branch at the current commit, then reset main back one. Nobody else is affected.",
        "confirm": "git status -sb"
      },
      {
        "id": "already-pushed",
        "cause": "The commit is on the remote and others may have pulled it.",
        "fix": "Do not reset and force-push. Use git revert to add a new commit that undoes it, then apply the original work on a proper branch.",
        "confirm": "git log --oneline {remote}/main -3"
      },
      {
        "id": "branch-exists",
        "cause": "A branch with that name is already present.",
        "fix": "Switch to it and cherry-pick the commit across, then reset main.",
        "confirm": "git branch --list {branch}"
      }
    ],
    "verify": [
      "git log on main matches the remote's main.",
      "Your commit appears in git log on {branch}.",
      "git status reports a clean working tree, or exactly the changes you stashed."
    ],
    "escalate": [
      "Output of git status -sb",
      "git log --oneline -5 for both branches",
      "Whether the commit was pushed, and whether anyone has pulled since"
    ]
  },
  {
    "id": "git-force-push-safely",
    "tier": "verified",
    "technology": "Git",
    "category": "Remotes",
    "title": "I need to force-push a rebased branch",
    "summary": "Rewriting a shared branch can silently destroy a colleague's commits. There is a safe form of this command.",
    "aliases": [
      "force push",
      "push -f",
      "rebase then push",
      "force-with-lease",
      "non-fast-forward",
      "rejected push"
    ],
    "verified_on": "2026-09-11",
    "verified_against": "git 2.45",
    "verified_by": "dinesh",
    "source": "https://git-scm.com/docs/git-push",
    "inputs": [
      {
        "key": "branch",
        "label": "Branch",
        "example": "feature/checkout-retry"
      },
      {
        "key": "remote",
        "label": "Remote",
        "example": "origin"
      }
    ],
    "steps": [
      {
        "title": "Check whether anyone else has pushed to this branch",
        "cmd": "git fetch {remote} && git log --oneline {remote}/{branch} -5",
        "risk": "safe",
        "look": "Commits you do not recognise. If someone else's work is on this branch, a force-push will delete it and they will not get a warning.",
        "signals": [
          {
            "match": "(?i)commit",
            "means": "Read the author of each. Anything not yours means stop and talk to them first.",
            "goto": "cause:shared-branch"
          }
        ]
      },
      {
        "title": "Force-push, but only if the remote is where you last saw it",
        "cmd": "git push --force-with-lease={branch} {remote} {branch}",
        "risk": "destructive",
        "note": "This rewrites remote history. --force-with-lease aborts if someone pushed since your last fetch, which plain --force does not. Never use plain --force on a shared branch. If this is a protected branch, it should be rejected by policy, not by your judgement.",
        "look": "A forced update line. If it is rejected with 'stale info', someone pushed after your fetch and the lease did its job.",
        "signals": [
          {
            "match": "(?i)stale info|rejected",
            "means": "The lease saved you. Someone pushed after your last fetch. Fetch again and re-examine before retrying.",
            "goto": "cause:raced"
          },
          {
            "match": "(?i)protected branch|pre-receive hook declined",
            "means": "Branch protection blocked it. That is working as intended; open a PR instead.",
            "goto": "cause:protected"
          }
        ]
      }
    ],
    "causes": [
      {
        "id": "shared-branch",
        "cause": "Other people have commits on this branch.",
        "fix": "Do not force-push. Merge their work in, or coordinate a window with them first.",
        "confirm": "git log --format='%an %s' {remote}/{branch} -10"
      },
      {
        "id": "raced",
        "cause": "Someone pushed between your fetch and your push.",
        "fix": "Fetch, inspect what arrived, rebase onto it, then retry with --force-with-lease.",
        "confirm": "git fetch {remote} && git log --oneline {remote}/{branch} -5"
      },
      {
        "id": "protected",
        "cause": "Branch protection rules forbid force-push.",
        "fix": "Open a pull request. If the rewrite is genuinely required, it needs a repo admin, not a workaround.",
        "confirm": "git ls-remote {remote} {branch}"
      }
    ],
    "verify": [
      "{remote}/{branch} points at your intended commit.",
      "No commits by other authors disappeared from the log.",
      "CI on the branch is green after the rewrite."
    ],
    "escalate": [
      "git log --oneline of local and remote branch",
      "The exact push output including any rejection reason",
      "Whether the branch is protected and who owns it"
    ]
  },
  {
    "id": "git-merge-conflict",
    "tier": "documented",
    "technology": "Git",
    "category": "Merging",
    "title": "Merge or rebase stopped with conflicts",
    "summary": "Git cannot combine two changes automatically and is waiting for you to decide.",
    "aliases": [
      "merge conflict",
      "conflict",
      "both modified",
      "fix conflicts and run commit",
      "rebase conflict",
      "unmerged paths"
    ],
    "source": "https://git-scm.com/docs/git-merge",
    "inputs": [
      {
        "key": "branch",
        "label": "Branch being merged",
        "example": "main"
      }
    ],
    "steps": [
      {
        "title": "See exactly which files are unmerged",
        "cmd": "git status --short",
        "risk": "safe",
        "look": "Lines starting UU are both-modified. AA is both-added, DU and UD are delete-versus-modify, which need a decision rather than an edit.",
        "signals": [
          {
            "match": "^UU ",
            "means": "Both sides changed the same file. Edit it and resolve.",
            "goto": "cause:both-modified"
          },
          {
            "match": "^(DU|UD) ",
            "means": "One side deleted what the other changed. Neither version is automatically right.",
            "goto": "cause:delete-modify"
          }
        ]
      },
      {
        "title": "Understand which operation you are inside",
        "cmd": "git status | head -3",
        "risk": "safe",
        "look": "Rebase and merge resolve identically but finish differently. Finishing the wrong way is where people lose work."
      },
      {
        "title": "See both sides of a conflicting file",
        "cmd": "git diff --diff-filter=U",
        "risk": "safe",
        "look": "The conflict markers with both versions. The section above ======= is your current branch, below it is what you are bringing in."
      },
      {
        "title": "Abort and return to where you started",
        "cmd": "git merge --abort",
        "risk": "destructive",
        "look": "A clean working tree back at the pre-merge commit. This is always safe for committed history, and is the right move when a merge has gone badly.",
        "note": "Discards the entire in-progress merge and any conflict resolution you have done so far. Committed work is untouched, but unsaved resolution effort is lost. Inside a rebase, the equivalent is git rebase --abort."
      }
    ],
    "causes": [
      {
        "id": "both-modified",
        "cause": "Both branches changed the same lines.",
        "fix": "Edit the file to the intended final state, remove the markers, git add it, then continue.",
        "confirm": ""
      },
      {
        "id": "delete-modify",
        "cause": "One branch deleted a file the other modified.",
        "fix": "Decide deliberately: git rm to accept the deletion, or git add to keep the modified version.",
        "confirm": ""
      },
      {
        "id": "wrong-finish",
        "cause": "Finished a rebase with git commit instead of git rebase --continue.",
        "fix": "Inside a rebase always use git rebase --continue. Inside a merge, git commit is correct.",
        "confirm": ""
      }
    ],
    "verify": [
      "git status reports a clean tree with no unmerged paths.",
      "The build and tests pass on the merged result.",
      "Neither side's intent was silently dropped."
    ],
    "escalate": [
      "git status --short output",
      "Whether it is a merge or a rebase",
      "Which files conflicted and how each was resolved"
    ]
  },
  {
    "id": "git-detached-head",
    "tier": "documented",
    "technology": "Git",
    "category": "Branches",
    "title": "You are in detached HEAD state",
    "summary": "Commits made here belong to no branch and become unreachable when you switch away.",
    "aliases": [
      "detached head",
      "not on any branch",
      "head detached at",
      "lost my commits",
      "commits disappeared after checkout"
    ],
    "source": "https://git-scm.com/docs/git-checkout",
    "inputs": [
      {
        "key": "branch",
        "label": "Branch to keep the work on",
        "example": "feature/fix-checkout"
      }
    ],
    "steps": [
      {
        "title": "Confirm the state and whether you have made commits",
        "cmd": "git status -sb && git log --oneline -5",
        "risk": "safe",
        "look": "'HEAD detached at' confirms it. If the log shows commits you made here, they exist but nothing points at them yet.",
        "signals": [
          {
            "match": "(?i)HEAD detached",
            "means": "Detached. Any commits here need a branch before you move.",
            "goto": "cause:detached"
          }
        ]
      },
      {
        "title": "Create a branch at your current position",
        "cmd": "git switch -c {branch}",
        "risk": "safe",
        "look": "Your commits are now on a real branch and safe. Do this before anything else, and before switching away."
      },
      {
        "title": "If you already switched away and lost them, find them in reflog",
        "cmd": "git reflog -20",
        "risk": "safe",
        "look": "Every position HEAD has held, with its SHA. Detached commits remain here for about ninety days, so almost nothing is truly lost."
      },
      {
        "title": "Recover a specific lost commit",
        "cmd": "git branch recovered <sha-from-reflog>",
        "risk": "state",
        "look": "A branch named recovered holding the work. Verify with git log before relying on it.",
        "note": "Creates a new branch pointing at that commit. Nothing existing is modified and no history is rewritten, but pick the SHA carefully or you will branch from the wrong point."
      }
    ],
    "causes": [
      {
        "id": "detached",
        "cause": "HEAD points at a commit rather than a branch.",
        "fix": "Create a branch here with git switch -c. Do it before switching away.",
        "confirm": ""
      },
      {
        "id": "already-switched",
        "cause": "You left detached HEAD and the commits seem gone.",
        "fix": "They are in the reflog. Find the SHA and branch from it.",
        "confirm": ""
      },
      {
        "id": "intentional",
        "cause": "You checked out a tag or old commit deliberately.",
        "fix": "Fine as long as you make no commits. If you do, branch first.",
        "confirm": ""
      }
    ],
    "verify": [
      "git status shows you on a named branch.",
      "git log on that branch contains your commits.",
      "The branch is pushed, or you know it is local only."
    ],
    "escalate": [
      "git status -sb output",
      "git reflog around the time the work was done",
      "Which commits are missing"
    ]
  },
  {
    "id": "git-large-file-rejected",
    "tier": "documented",
    "technology": "Git",
    "category": "Remotes",
    "title": "Push rejected for file size",
    "summary": "The remote refuses a file over its limit. Deleting it in a new commit does not help; it is still in history.",
    "aliases": [
      "file too large",
      "push rejected size",
      "large file",
      "gh001",
      "exceeds file size limit",
      "git lfs"
    ],
    "source": "https://docs.github.com/en/repositories/working-with-files/managing-large-files",
    "inputs": [
      {
        "key": "file",
        "label": "The oversized file",
        "example": "data/dump.sql"
      }
    ],
    "steps": [
      {
        "title": "Read the rejection and identify the file and commit",
        "cmd": "git push 2>&1 | more",
        "risk": "safe",
        "look": "The remote names the file and the commit that introduced it. That commit is what must change, not your latest one.",
        "signals": [
          {
            "match": "(?i)exceeds|too large|GH001",
            "means": "Size limit. The file is embedded in history and must be removed from it.",
            "goto": "cause:in-history"
          }
        ]
      },
      {
        "title": "Find the largest objects actually in history",
        "cmd": "git rev-list --objects --all | git cat-file --batch-check=\"%(objecttype) %(objectname) %(objectsize) %(rest)\" | findstr blob",
        "risk": "safe",
        "look": "Sizes are in bytes. This shows what is really in the repository, which is often not what you expected."
      },
      {
        "title": "If it is only in your last commit, remove it there",
        "cmd": "git rm --cached {file} && git commit --amend --no-edit",
        "risk": "destructive",
        "look": "git show --stat no longer lists the file. The file remains on disk because only the index entry was removed.",
        "note": "Rewrites your most recent commit. Safe if that commit has not been pushed. If it has been pushed and others have pulled it, do not amend — the file needs removing from shared history through a coordinated rewrite instead."
      }
    ],
    "causes": [
      {
        "id": "in-history",
        "cause": "The file is in an earlier commit, so deleting it now changes nothing.",
        "fix": "It must be removed from history. For a single unpushed commit, amend. For anything shared, coordinate a rewrite with the repository owner.",
        "confirm": ""
      },
      {
        "id": "should-be-lfs",
        "cause": "The file is a legitimate large asset.",
        "fix": "Track it with Git LFS and add the pattern to .gitattributes before committing it again.",
        "confirm": ""
      },
      {
        "id": "should-not-be-committed",
        "cause": "A build artifact or database dump got committed.",
        "fix": "Add it to .gitignore. Artifacts belong in a registry, not in Git.",
        "confirm": ""
      }
    ],
    "verify": [
      "The push is accepted.",
      "The repository size is reasonable for a fresh clone.",
      ".gitignore or .gitattributes prevents a recurrence."
    ],
    "escalate": [
      "The verbatim rejection message",
      "Which commit introduced the file",
      "Whether that commit has been pushed and pulled by others"
    ]
  },
  {
    "id": "git-wrong-author",
    "tier": "documented",
    "technology": "Git",
    "category": "History & Undo",
    "title": "Commits show the wrong author or email",
    "summary": "Work commits attributed to a personal address, or the reverse. Usually a missing per-repository config.",
    "aliases": [
      "wrong author",
      "wrong email commit",
      "commits not showing github",
      "unverified commit",
      "wrong git config",
      "author email"
    ],
    "source": "https://git-scm.com/docs/git-config",
    "inputs": [
      {
        "key": "email",
        "label": "Correct email",
        "example": "dinesh@example.com"
      },
      {
        "key": "name",
        "label": "Correct name",
        "example": "Dinesh Ilangovan"
      }
    ],
    "steps": [
      {
        "title": "See what identity the recent commits carry",
        "cmd": "git log --format='%h %an <%ae>' -5",
        "risk": "safe",
        "look": "The email is what matters for attribution on the hosting side. The display name is cosmetic."
      },
      {
        "title": "See which config is being used and from where",
        "cmd": "git config --show-origin user.email && git config --show-origin user.name",
        "risk": "safe",
        "look": "The file path in the output tells you whether this is global or repository-local. A missing local value means the global one applies."
      },
      {
        "title": "Set the correct identity for this repository only",
        "cmd": "git config user.email \"{email}\" && git config user.name \"{name}\"",
        "risk": "state",
        "look": "git config --show-origin now points at the repository's own config file.",
        "note": "Writes to .git/config for this repository only. Existing commits are unaffected — this changes future commits, nothing more. Setting it locally is the right move when you mix work and personal repositories on one machine."
      },
      {
        "title": "Fix the identity on the most recent commit",
        "cmd": "git commit --amend --reset-author --no-edit",
        "risk": "destructive",
        "look": "git log shows the corrected author on the top commit.",
        "note": "Rewrites the last commit, changing its SHA. Safe while unpushed. If it has been pushed, this creates a divergence that requires a force-push, which rewrites shared history."
      }
    ],
    "causes": [
      {
        "id": "no-local-config",
        "cause": "No repository-local identity, so a global one applied.",
        "fix": "Set user.email locally in each work repository. Consider clearing the global value so it can never apply silently.",
        "confirm": ""
      },
      {
        "id": "already-pushed",
        "cause": "The wrong-author commits are already on the remote.",
        "fix": "Leave them. Rewriting shared history to fix a display name is not a worthwhile trade.",
        "confirm": ""
      },
      {
        "id": "unverified",
        "cause": "The email is right but commits show as unverified.",
        "fix": "That is signing, not authorship. Set up commit signing separately.",
        "confirm": ""
      }
    ],
    "verify": [
      "New commits carry the correct author.",
      "git config --show-origin points at the local config.",
      "No shared history was rewritten to achieve it."
    ],
    "escalate": [
      "git log with author format",
      "Output of git config --show-origin for user.email",
      "Whether the affected commits are pushed"
    ]
  }
]